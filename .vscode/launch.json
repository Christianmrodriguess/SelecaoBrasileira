{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Executar programa",
      "type": "cppdbg",
      "request": "launch",
      "program": "C:\\Windows\\System32\\cmd.exe",
      "args": [
        "/k",
        "${workspaceFolder}\\build\\${fileBasenameNoExtension}.exe && echo. && echo. && echo. && echo. && echo. && pause && exit"
      ],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "externalConsole": true,
      "MIMode": "gdb",
      "miDebuggerPath": "C:\\MinGW\\bin\\gdb.exe",
      "preLaunchTask": "Compilar C"
    }
  ]
}